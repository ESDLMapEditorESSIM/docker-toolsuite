apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mapeditor-tls-certificate
spec:
  secretName: mapeditor-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: {{ include "mapeditor.host" . }}
  dnsNames:
    - {{ include "mapeditor.host" . }}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  creationTimestamp: null
  labels:
    app: keycloak-claim0
  name: keycloak-claim0
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1000Mi
  # storageClassName: azurefile-csi
status: {}
